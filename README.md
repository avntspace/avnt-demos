# AVNT Demos

Centralized sales demos for brand-specific bundle and cart optimization concepts.

Live paths:
- `/trapstar/`
- `/esenes/`

Each brand folder contains a self-contained static demo with its own `index.html`, `styles.css`, `app.js`, and brand assets.

